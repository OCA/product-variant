The merging options are quite limited when it comes to keeping the original
record values. To ensure that the original variant info is preserved, go to
*Settings > Technical > Parameters > System Parameters* and add the fields you
want to keep to the `product_variant_template_reassign.keep_fields` parameter
(comma-separated field names, `list_price,standard_price` by default).
